; infinitag-now-core – nur für die nativen Unit-Tests der Protokoll-Lib.
; In Geräte-Firmwares wird die Lib per lib_deps eingebunden (siehe README).

[env:native]
platform = native
test_framework = unity
test_build_src = yes
build_flags = -std=c++17 -Wall -Isrc
